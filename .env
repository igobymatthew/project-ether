# Common
PROJECT_NAME=project-ether
FRONTEND_PORT=8080
API_PORT=8000
TTS_PORT=8010

# API (LLM / ASR)
# Place your GGUF under ./models on the host
ETHER_LLM_MODEL=/models/llama-7b.Q4_K_M.gguf
ALLOW_ORIGINS=http://localhost:${FRONTEND_PORT},http://127.0.0.1:${FRONTEND_PORT}

# TTS
# Choose BACKEND: fallback | browser | chatterbox
TTS_BACKEND=fallback

# Torch variant for TTS image: cpu | cu121
TORCH_VARIANT=cpu
# If TORCH_VARIANT=cu121, you’ll need NVIDIA drivers + Docker w/ GPU support
